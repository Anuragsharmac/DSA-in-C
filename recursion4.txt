#include <stdio.h>
int main()
{
      int a,b;
      printf("enter the numbers :");
      scanf("%d %d",&a,&b);
      int x= gcd(a,b);
      printf("hcf of %d and %d is %d",a,b,x);
}
int gcd(int a,int b)
{
      if(a%b==0)
      return b;
      else 
      return gcd(b,a%b);
}